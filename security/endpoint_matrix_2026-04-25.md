| Endpoint/Function | Auth required? | Role check? | Tenant check? | Input validation? | Uses service role? | Risk | Fix |
|---|---|---|---|---|---|---|---|
| POST /mobile-events (ingest_mobile_events) | yes | no/implicit | no/implicit | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| POST / (chat) | yes | no/implicit | yes | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| GET /history (chat_history) | yes | no/implicit | yes | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| GET /requests/{request_id}/events (chat_request_events) | yes | no/implicit | yes | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| POST /stream (chat_stream) | yes | no/implicit | yes | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| GET /today (get_today_checkin) | yes | no/implicit | yes | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| GET /previous (get_previous_checkin) | yes | no/implicit | yes | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| GET /progress (get_checkin_progress) | yes | no/implicit | yes | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| POST / (submit_daily_checkin) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /generate-plan (generate_checkin_plan) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /log-workout (log_generated_workout) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /bootstrap (get_onboarding_bootstrap) | yes | no/implicit | no/implicit | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| POST /role (post_onboarding_role) | yes | no/implicit | no/implicit | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| PATCH /state (patch_onboarding_state) | yes | no/implicit | no/implicit | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| POST /complete (complete_onboarding) | yes | no/implicit | no/implicit | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| GET /active (get_active_plan) | yes | no/implicit | yes | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| POST /generate (generate_plan) | yes | no/implicit | yes | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| GET /me (get_my_profile) | yes | no/implicit | yes | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| PATCH /me (patch_my_profile) | yes | no/implicit | yes | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| GET /me/trainer-schedule (get_my_trainer_schedule) | yes | no/implicit | yes | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| GET /status (get_assignment_status) | yes | no/implicit | yes | partial | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| POST /assign (assign_trainer) | yes | no/implicit | yes | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| POST /assign-by-invite (assign_trainer_by_invite) | yes | no/implicit | yes | yes | yes | Medium: admin-backed path without explicit role assertion in handler | Add explicit role/actor assertion or document mixed-role intent + regression tests. |
| GET /bootstrap (bootstrap_trainer_assistant) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /execute (execute_trainer_assistant) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /execute/stream (execute_trainer_assistant_stream) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /drafts/{draft_id}/edit (edit_trainer_assistant_draft) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /drafts/{draft_id}/approve (approve_trainer_assistant_draft) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /drafts/{draft_id}/reject (reject_trainer_assistant_draft) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /background/run (run_trainer_assistant_background) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET / (list_trainer_clients) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /invite-codes (list_trainer_client_invite_codes) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /invite-codes (create_trainer_client_invite_code) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| DELETE /invite-codes/{invite_id} (deactivate_trainer_client_invite_code) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| PATCH /{client_id} (patch_trainer_client) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| DELETE /{client_id} (delete_trainer_client) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /{client_id}/detail (get_trainer_client_detail) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /{client_id}/memory (list_trainer_client_memory) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /{client_id}/memory (create_trainer_client_memory) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| PATCH /{client_id}/memory/{memory_id} (patch_trainer_client_memory) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| DELETE /{client_id}/memory/{memory_id} (archive_trainer_client_memory) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /{client_id}/ai-context (get_trainer_client_ai_context) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| PATCH /{client_id}/meeting-location (patch_trainer_client_meeting_location) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /{client_id}/schedule-preferences (get_trainer_client_schedule_preferences) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| PATCH /{client_id}/schedule-preferences (patch_trainer_client_schedule_preferences) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /{client_id}/schedule-exceptions (create_trainer_client_schedule_exception) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| DELETE /{client_id}/schedule-exceptions/{session_date} (delete_trainer_client_schedule_exception) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /workspace (get_trainer_coach_workspace) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /queue (get_trainer_coach_queue) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /events (get_trainer_coach_events) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /events (create_trainer_coach_event) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /queue/{output_id}/approve (approve_trainer_coach_queue_item) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /queue/{output_id}/edit (edit_trainer_coach_queue_item) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /queue/{output_id}/reject (reject_trainer_coach_queue_item) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /today (get_trainer_home_today) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /command-center (get_trainer_command_center) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET / (list_documents) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST / (create_document) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /ingest (ingest_document) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /rules (list_rules) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| PATCH /rules/{rule_id} (update_rule) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| DELETE /rules/{rule_id} (archive_rule) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /entries (list_entries) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /entries/classify (classify_entry) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /entries (create_entry) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| PATCH /entries/{entry_id} (update_entry) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| DELETE /entries/{entry_id} (archive_entry) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /entries/{entry_id}/refine (refine_entry) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /entries/{entry_id}/versions (list_entry_versions) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| PATCH /{document_id} (update_document) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| DELETE /{document_id} (delete_document) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET / (list_personas) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST / (create_persona) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /templates (list_trainer_program_templates) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /templates (create_trainer_program_template) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| PATCH /templates/{template_id} (patch_trainer_program_template) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /templates/{template_id}/archive (archive_trainer_program_template) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /queue (get_review_queue) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /queue/{queue_id}/approve (approve_queue_item) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /outputs (get_review_outputs) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /outputs/{output_id} (get_review_output_detail) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /outputs/{output_id}/edit (edit_review_output) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /outputs/{output_id}/approve (approve_review_output) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /outputs/{output_id}/reject (reject_review_output) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| GET /me (get_my_trainer_settings) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| PATCH /me (patch_my_trainer_settings) | yes | yes | yes | yes | yes | Medium: service-role dependency, relies on app-layer checks | Keep app-layer authz (`require_trainer_actor`/`authorize_actor_access`) and add cross-tenant tests. |
| POST /generate (generate_workout) | yes | no/implicit | no/implicit | yes | no | Low: user-scoped client + context checks | Retain user-scoped client access; enforce tenant context if expanded. |
